document.addEventListener('DOMContentLoaded', function(){ 

  var options = {
    html5: {
      hlsjsConfig: {},
    },
    dnaConfig: {},
  };

  var player = videojs("player", options);

}, false);